[phases.setup]
nixPkgs = ["php83", "php83Extensions.pcntl"]

[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader"]

[start]
cmd = "php artisan reverb:start --host=0.0.0.0 --port=8080"